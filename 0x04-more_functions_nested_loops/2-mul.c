#include "main.h"

/**
 *mul - This is the function
 *@a: this is the variable
 *@b: this is the variable
 *Return: always returns 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
